/** Must match the bundled asset filename and backend Expo push `sound` field. */
export const APP_NOTIFICATION_SOUND = 'notification_ping.mp3';

/** Bump when changing Android channel sound so devices pick up the new tone. */
export const APP_NOTIFICATION_CHANNEL_ID = 'datamart_alerts_v2';
